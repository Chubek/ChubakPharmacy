<?php
/**
 * @var \Phalcon\Mvc\View\Engine\Php $this
 */
?>

<?php use Phalcon\Tag; ?>

<div class="row">
    <nav>
        <ul class="pager">
            <li class="previous"><?php echo $this->tag->linkTo(["Payments/index", "Go Back"]); ?></li>
            <li class="next"><?php echo $this->tag->linkTo(["Payments/new", "Create "]); ?></li>
        </ul>
    </nav>
</div>

<div class="page-header">
    <h1>Search result</h1>
</div>

<?php echo $this->getContent(); ?>

<div class="row">
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Payment</th>
            <th>Payment Of Date</th>
            <th>Payment Of Method</th>
            <th>Payment Of Amount</th>

                <th></th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        <?php foreach ($page->items as $Payment): ?>
            <tr>
                <td><?php echo $Payment->payment_id ?></td>
            <td><?php echo $Payment->payment_date ?></td>
            <td><?php echo $Payment->payment_method ?></td>
            <td><?php echo $Payment->payment_amount ?></td>

                <td><?php echo $this->tag->linkTo(["Payments/edit/" . $Payment->payment_id, "Edit"]); ?></td>
                <td><?php echo $this->tag->linkTo(["Payments/delete/" . $Payment->payment_id, "Delete"]); ?></td>
            </tr>
        <?php endforeach; ?>
        </tbody>
    </table>
</div>

<div class="row">
    <div class="col-sm-1">
        <p class="pagination" style="line-height: 1.42857;padding: 6px 12px;">
            <?php echo $page->current, "/", $page->total_pages ?>
        </p>
    </div>
    <div class="col-sm-11">
        <nav>
            <ul class="pagination">
                <li><?php echo $this->tag->linkTo("Payments/search", "First") ?></li>
                <li><?php echo $this->tag->linkTo("Payments/search?page=" . $page->before, "Previous") ?></li>
                <li><?php echo $this->tag->linkTo("Payments/search?page=" . $page->next, "Next") ?></li>
                <li><?php echo $this->tag->linkTo("Payments/search?page=" . $page->last, "Last") ?></li>
            </ul>
        </nav>
    </div>
</div>
